import { LuSettings2 } from "react-icons/lu";
const FilterButton = () => {
  return (
    // <IconButton
    //   colorScheme="gray"
    //   aria-label="Filtres"
    //   icon={<SearchIcon />}
    //   onClick={handleSearch}
    // />
    <LuSettings2 />
  );
};

export default FilterButton;
